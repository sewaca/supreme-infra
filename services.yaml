# Список всех сервисов в проекте
# Используется генераторами для автоматического изменения конфигураций

services:
  nest:
    - name: backend
      description: Backend service (NestJS)
      database:
        enabled: true
        name: backend_db
        user: backend_user
        passwordSecret: DB_PASSWORD # GitHub Secret name for database password

    # - name: auth-bff
    #   description: Auth BFF service (NestJS)
    #   database:
    #     enabled: true
    #     name: auth_bff_db
    #     user: auth_bff_user
    #     passwordSecret: AUTH_BFF_DB_PASSWORD # GitHub Secret name for database password

  next:
    - name: frontend
      description: Frontend service (Next.js)
