services:
  nest:
    - name: core-auth-bff
      description: "core-auth-bff "
      database:
        enabled: true
        name: core_auth_bff_db
        user: core_auth_bff_user
        passwordSecret: DB_PASSWORD
    - name: core-recipes-bff
      description: "recipes backend "
      database:
        enabled: true
        name: core_recipes_bff_db
        user: core_recipes_bff_user
        passwordSecret: DB_PASSWORD
  next:
    - name: frontend
      description: Frontend service (Next.js)
    - name: auth-ssr
      description: authorization-ssr-sservice
    # ! service is empty now and will not be used for now
    # - name: web-documents-ssr
    #   description: "documents presentation "
    - name: web-profile-ssr
      description: "profile "
  fastapi:
    - name: core-client-info
      description: "Client information service — user profile, settings, ratings, achievements, grades, references, orders"
      database:
        enabled: true
        name: core_client_info_db
        user: core_client_info_user
        passwordSecret: DB_PASSWORD
