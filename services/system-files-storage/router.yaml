# Generated router configuration for system-files-storage service
# auth_level and public are manually maintained — use pnpm run generate:router to update routes.

service: system-files-storage
type: fastapi
routes:
  - path: /system-files-storage/upload
    method: POST
    auth_level: valid
    public: true
