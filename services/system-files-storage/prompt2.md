подключи system-files-storage к web-messages-ssr + core-messages при отправке файла в сообщении

логика такая:

1. пользователь грузит файл в новый сервис
2. получает в ответ ссылку на него
3. отправляет сообщение, которое будет отображаться как файл, сохраняя свое изначальное имя и показывающее размер файла

ограничения должны быть такими:

1. файл не тяжелее 10 МБ
2. рейт лимитер на количество загруженных файлов (думаю тут надо сделать гибкий рейт лимитер, чтобы рос экспоненциально)
