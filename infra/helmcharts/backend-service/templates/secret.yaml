{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "service.fullname" . }}-secrets
  labels:
    {{- include "service.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}

