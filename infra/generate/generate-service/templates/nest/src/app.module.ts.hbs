import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { {{#if hasDatabase}}createDatabaseImports, {{/if}}LoggerModule } from '@supreme-int/nestjs-shared';
import { HealthController } from './features/HealthCheck/api/health.controller';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    LoggerModule,{{#if hasDatabase}}
    ...createDatabaseImports(),{{/if}}
  ],
  controllers: [HealthController],
  providers: [],
})
export class AppModule {}

