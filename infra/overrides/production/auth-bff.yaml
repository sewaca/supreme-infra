# This file is automatically generated and should not be edited manually!
# Generated values for auth-bff in production environment
# Source: services/auth-bff/service.yaml
replicaCount: 1
image:
  repository: auth-bff
  tag: ""
  pullPolicy: IfNotPresent
nameOverride: auth-bff
fullnameOverride: auth-bff
service:
  type: ClusterIP
  port: 80
  targetPort: 4001
metrics:
  enabled: true
  port: 9464
env:
  PORT: "4001"
  NODE_ENV: production
  LOKI_ENDPOINT: http://loki-gateway.monitoring.svc.cluster.local/otlp/v1/logs
  DB_HOST: postgresql-auth_bff_db
  DB_PORT: "5432"
  DB_NAME: auth_bff_db
  DB_USER: auth_bff_user
secrets: {}
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 150m
    memory: 300Mi
livenessProbe:
  httpGet:
    path: /auth-bff/api/status
    port: http
  initialDelaySeconds: 30
  periodSeconds: 20
  timeoutSeconds: 3
  failureThreshold: 5
readinessProbe:
  httpGet:
    path: /auth-bff/api/status
    port: http
  initialDelaySeconds: 30
  periodSeconds: 20
  timeoutSeconds: 3
  failureThreshold: 5
nodeSelector: {}
tolerations: []
affinity: {}
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
        - type: Pods
          value: 1
          periodSeconds: 60
      selectPolicy: Min
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 100%
canary:
  enabled: true
  replicas: 1
  image:
    repository: ""
    tag: ""
replicas: 1
