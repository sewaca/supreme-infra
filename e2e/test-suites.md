# E2E: тест-сьюты (`e2e/src/tests/**/*.spec.ts`)

Сводка по состоянию на момент генерации файла.

---

## `login/login.spec.ts`

### `Login page — unauthenticated`

| Тест                                          | Описание                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------ |
| `loads with correct heading and subtitle`     | Заголовок «Вход в личный кабинет» и подзаголовок                         |
| `shows email and password fields`             | Поля email и пароля                                                      |
| `shows submit button`                         | Кнопка отправки с текстом «Войти»                                        |
| `shows links to register and forgot password` | Ссылки на `/register` и `/forgot-password`                               |
| `logs in with correct credentials`            | Успешный вход (мок ipregistry), редирект на `/profile`, имя пользователя |
| `shows error alert on invalid credentials`    | Алерт при неверных учётных данных                                        |

---

## `references/references.typeInput.spec.ts`

### `1. Тип справки`

| Тест                                                                              | Описание |
| --------------------------------------------------------------------------------- | -------- |
| `1.1 — валидный тип «По месту работы родителей» разблокирует поле выдачи`         |          |
| `1.2 — валидный тип «РЖД» разблокирует поле выдачи`                               |          |
| `1.3 — тип с пробелами по краям принимается корректно`                            |          |
| `1.4 — пользовательский тип «Справка для архива» принимается (freeSolo)`          | + PDF    |
| `1.5 — пустой тип держит кнопку «Заказать» disabled`                              |          |
| `1.6 — тип из одних пробелов держит кнопку «Заказать» disabled`                   |          |
| `1.7 — промежуточный ввод «По» показывает отфильтрованные варианты в выпадашке`   |          |
| `1.8 — ввод в нижнем регистре «по месту работы» принимается (регистронезависимо)` | + PDF    |

---

## `references/references.pickup.spec.ts`

### `2. Где получить справку`

| Тест                                                                   | Описание        |
| ---------------------------------------------------------------------- | --------------- |
| `2.1 — пункт «СПбГУТ» выбирается после указания типа`                  |                 |
| `2.2 — пункт «СПбКТ» выбирается после указания типа`                   |                 |
| `2.3 — поле выдачи недоступно до выбора типа`                          | `aria-disabled` |
| `2.4 — кнопка disabled без пункта выдачи при выключенном PDF`          |                 |
| `2.5 — после выбора типа автоматически выбирается первый пункт выдачи` | тип «РЖД»       |
| `2.6 — поле выдачи скрыто при включённом PDF`                          |                 |

---

## `references/references.pdf.spec.ts`

### `3. PDF-переключатель`

| Тест                                                   | Описание |
| ------------------------------------------------------ | -------- |
| `3.1 — включение PDF скрывает поле выдачи`             |          |
| `3.2 — PDF выключен по умолчанию; поле выдачи видно`   |          |
| `3.3 — переключение false→true скрывает поле выдачи`   |          |
| `3.4 — переключение true→false возвращает поле выдачи` |          |

---

## `references/references.orderButton.spec.ts`

### `4. Кнопка «Заказать»`

| Тест                                                                             | Описание                |
| -------------------------------------------------------------------------------- | ----------------------- |
| `4.1 — disabled: тип не выбран, PDF выключен`                                    |                         |
| `4.2 — disabled: тип выбран, пункт выдачи не выбран, PDF выключен`               | freeSolo без автовыбора |
| `4.3 — active: тип выбран из справочника, пункт выдачи автовыбран, PDF выключен` | «РЖД»                   |
| `4.4 — active: тип выбран, PDF включён (пункт выдачи не нужен)`                  |                         |
| `4.5 — disabled: тип пустой, PDF включён`                                        |                         |
| `4.6 — disabled: тип из только пробелов, PDF включён`                            |                         |

---

## `references/references.history.spec.ts`

### `5. Фильтрация списка типов`

| Тест                                                      | Описание  |
| --------------------------------------------------------- | --------- |
| `5.1 — ввод «По» показывает только типы, содержащие «По»` |           |
| `5.2 — ввод «по» фильтрует без учёта регистра`            |           |
| `5.3 — ввод «zzz» добавляет пользовательскую опцию`       |           |
| `5.4 — пустой ввод показывает все предустановленные типы` | ≥ 5 опций |

### `6. История заказов`

| Тест                                                                                        | Описание                |
| ------------------------------------------------------------------------------------------- | ----------------------- |
| `6.1 — секция «История заказов» присутствует на странице`                                   |                         |
| `6.2 — фильтр статусов отображает все пять кнопок`                                          |                         |
| `6.3 — по умолчанию активен фильтр «Все»`                                                   |                         |
| `6.4 — клик по фильтру переключает активную кнопку`                                         |                         |
| `6.5 — пустое состояние показывает нужный текст`                                            | если нет карточек       |
| `6.6 — клик по карточке открывает модальное окно`                                           | skip, если нет карточек |
| `6.7 — модальное окно закрывается по кнопке «Закрыть»`                                      | skip, если нет карточек |
| `6.8 — модальное окно содержит дату заказа и статус`                                        | skip, если нет карточек |
| `6.9 — фильтр «Подготовка» показывает только соответствующие карточки или пустое состояние` |                         |
| `6.10 — фильтр «Все» возвращает полный список`                                              |                         |

---

## Краткая сводка

| Файл                                        | Сьютов | Тестов (всего) |
| ------------------------------------------- | ------ | -------------- |
| `login/login.spec.ts`                       | 1      | 6              |
| `references/references.typeInput.spec.ts`   | 1      | 8              |
| `references/references.pickup.spec.ts`      | 1      | 6              |
| `references/references.pdf.spec.ts`         | 1      | 4              |
| `references/references.orderButton.spec.ts` | 1      | 6              |
| `references/references.history.spec.ts`     | 2      | 14             |
| **Итого**                                   | **7**  | **44**         |
