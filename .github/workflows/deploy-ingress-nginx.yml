name: Deploy Ingress NGINX

on:
  workflow_dispatch:

env:
  HELM_VERSION: 'v3.13.0'
  INGRESS_NAMESPACE: 'ingress-nginx'
  INGRESS_RELEASE_NAME: 'ingress-nginx'
  SERVICE_NAME: 'ingress-nginx'

jobs:
  get-latest-version:
    name: Get latest Ingress NGINX release version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      release_tag: ${{ steps.get-version.outputs.release_tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get latest release version
        id: get-version
        run: |
          # Get all tags for ingress-nginx, sorted by version
          ALL_TAGS=$(git tag -l "ingress-nginx-v*" 2>/dev/null | grep -E "^ingress-nginx-v[0-9]+\.[0-9]+\.[0-9]+$" | sort -V || echo "")

          echo "All tags for ingress-nginx:"
          echo "$ALL_TAGS"

          LATEST_TAG=$(echo "$ALL_TAGS" | tail -n 1)

          if [ -z "$LATEST_TAG" ]; then
            VERSION="0.0.0"
            RELEASE_TAG=""
            echo "No previous release found for ingress-nginx, starting from version: $VERSION"
          else
            VERSION=$(echo "$LATEST_TAG" | sed "s/^ingress-nginx-v//")
            RELEASE_TAG="$LATEST_TAG"
            echo "Latest release for ingress-nginx: $LATEST_TAG, version: $VERSION"
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "release_tag=$RELEASE_TAG" >> $GITHUB_OUTPUT

  calculate-new-version:
    name: Calculate new Ingress NGINX version
    needs: get-latest-version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.calculate-version.outputs.version }}
    steps:
      - name: Calculate new version
        id: calculate-version
        env:
          CURRENT_VERSION: ${{ needs.get-latest-version.outputs.version }}
        run: |
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"
          
          # Simple patch version increment for Ingress NGINX
          NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
          
          echo "Version bump: $CURRENT_VERSION -> $NEW_VERSION"
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          
          echo "$NEW_VERSION" > "new_version.txt"

      - name: Upload version file
        uses: actions/upload-artifact@v4
        with:
          name: new-version
          path: new_version.txt

  create-release-branch:
    name: Create release branch
    needs: calculate-new-version
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download version file
        uses: actions/download-artifact@v4
        with:
          name: new-version

      - name: Configure Git
        run: |
          git config --global user.email "246592117+madara-robot@users.noreply.github.com"
          git config --global user.name "madara-robot"

      - name: Create release branch
        run: |
          VERSION=$(cat "new_version.txt")
          BRANCH_NAME="releases/production/ingress-nginx-${VERSION}"

          # Fetch all branches
          git fetch origin

          if git ls-remote --heads origin "$BRANCH_NAME" | grep -q "$BRANCH_NAME"; then
            echo "Branch $BRANCH_NAME already exists, skipping creation"
          else
            echo "Creating new branch $BRANCH_NAME from main"
            git checkout main
            git pull origin main
            git checkout -b "$BRANCH_NAME"
            git push -u origin "$BRANCH_NAME"
            echo "Created branch $BRANCH_NAME"
          fi

  deploy-ingress-nginx:
    name: Deploy Ingress NGINX
    needs: [calculate-new-version, create-release-branch]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Yandex Cloud
        uses: ./.github/workflows/jobs/setup-yandex-cloud
        with:
          yc-sa-json-credentials: ${{ secrets.YC_SA_JSON_CREDENTIALS }}
          yc-cloud-id: ${{ secrets.YC_CLOUD_ID }}
          yc-folder-id: ${{ secrets.YC_FOLDER_ID }}
          yc-k8s-cluster-id: ${{ secrets.YC_K8S_CLUSTER_ID }}

      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: ${{ env.HELM_VERSION }}

      - name: Add Ingress NGINX Helm repository
        run: |
          helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
          helm repo update

      - name: Update Helm dependencies
        run: |
          helm dependency update ./infra/helmcharts/ingress-nginx

      - name: Create or update namespace
        run: |
          kubectl create namespace ${{ env.INGRESS_NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy Ingress NGINX with Helm
        run: |
          CHART_PATH="./infra/helmcharts/ingress-nginx"
          
          echo "Deploying Ingress NGINX..."
          echo "Chart: $CHART_PATH"
          echo "Namespace: ${{ env.INGRESS_NAMESPACE }}"
          
          helm upgrade ${{ env.INGRESS_RELEASE_NAME }} $CHART_PATH \
            --namespace ${{ env.INGRESS_NAMESPACE }} \
            --install \
            --wait \
            --timeout 5m
          
          echo "Ingress NGINX deployment completed"

      - name: Verify Ingress NGINX deployment
        run: |
          echo "Checking Ingress NGINX controller pods..."
          kubectl get pods -n ${{ env.INGRESS_NAMESPACE }} -l app.kubernetes.io/name=ingress-nginx
          
          echo ""
          echo "Checking Ingress NGINX service..."
          kubectl get svc -n ${{ env.INGRESS_NAMESPACE }}
          
          echo ""
          echo "Checking Ingress resources..."
          kubectl get ingress -n default

  create-github-release:
    name: Create GitHub release
    needs: [calculate-new-version, create-release-branch, deploy-ingress-nginx]
    if: |
      always() &&
      needs.deploy-ingress-nginx.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download version file
        uses: actions/download-artifact@v4
        with:
          name: new-version

      - name: Install GitHub CLI
        uses: ./.github/workflows/jobs/install-github-cli

      - name: Prepare release info
        id: get-release-info
        run: |
          VERSION=$(cat "new_version.txt")
          TAG_NAME="ingress-nginx-v${VERSION}"
          RELEASE_NAME="Release Ingress NGINX v${VERSION}"

          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "release_name=$RELEASE_NAME" >> $GITHUB_OUTPUT

          {
            echo "# $RELEASE_NAME"
            echo ""
            echo "Ingress NGINX deployment completed successfully."
            echo ""
            echo "## Changes"
            echo "Ingress NGINX controller and routing rules have been deployed to the \`${{ env.INGRESS_NAMESPACE }}\` namespace."
            echo ""
            echo "## Deployment Details"
            echo "- **Namespace**: \`${{ env.INGRESS_NAMESPACE }}\`"
            echo "- **Release Name**: \`${{ env.INGRESS_RELEASE_NAME }}\`"
            echo "- **Controller**: Ingress NGINX"
          } > "release_body.txt"

      - name: Create GitHub Release
        env:
          TAG_NAME: ${{ steps.get-release-info.outputs.tag_name }}
          RELEASE_NAME: ${{ steps.get-release-info.outputs.release_name }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=$(cat "new_version.txt")
          BRANCH_NAME="releases/production/ingress-nginx-${VERSION}"
          BODY_FILE="release_body.txt"

          if gh release view "$TAG_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $TAG_NAME already exists, skipping"
            exit 0
          fi

          echo "Creating release $TAG_NAME from branch $BRANCH_NAME"
          gh release create "$TAG_NAME" \
            --title "$RELEASE_NAME" \
            --notes-file "$BODY_FILE" \
            --target "$BRANCH_NAME" \
            --repo "$GITHUB_REPOSITORY"

